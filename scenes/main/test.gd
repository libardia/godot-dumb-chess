extends Control

#
#func _ready() -> void:
    #for c: Board.Coord in Board.Coord.values():
        #var rf := Board.coord_to_rf(c)
        #var c2 := Board.rf_to_coord(rf)
        #print("%s, %s, %s" % [
                #Board.Coord.keys()[c],
                #rf,
                #Board.Coord.keys()[c2],
            #]
        #)
